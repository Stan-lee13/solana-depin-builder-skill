[workspace]
members = ["programs/depin-registry"]
resolver = "2"

[workspace.dependencies]
anchor-lang = { version = "0.30.1", features = ["init-if-needed"] }

[profile.release]
overflow-checks = true
lto = "fat"
codegen-units = 1

[profile.test]
opt-level = 1
